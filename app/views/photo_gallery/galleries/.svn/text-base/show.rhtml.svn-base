<%= path_links %>
<%= link_to('(add another photo)', :controller => '/photo_gallery/admin/photos', :action => 'new', :gallery_id => @gallery.id) if @admin %>
<br /><br />

<% @galleries.each do |gallery| %>
  <%= gallery_thumb(gallery) %>
<% end %>

<% @photos.each do |photo| %>
  <%= photo_thumb(photo) %>
<% end %>
